import SwiftUI

struct TrainView: View {
    var body: some View {
        NavigationStack {
            Text("Train")
                .navigationTitle("Train")
        }
    }
}

#Preview {
    TrainView()
}
