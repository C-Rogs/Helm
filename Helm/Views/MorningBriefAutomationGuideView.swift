import DesignSystem
import SwiftUI

/// Compatibility wrapper; Settings now opens `NotificationsSettingsView`.
struct MorningBriefAutomationGuideView: View {
    var body: some View {
        NotificationsSettingsView()
    }
}
